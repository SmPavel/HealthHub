<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthHub"
        tools:targetApi="31"
        >
        <!-- Other components and configuration settings -->

        <activity android:name=".Activity.MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity.RegisterActivity" />

        <activity android:name=".Activity.LoginActivity" />

        <activity android:name=".Activity.HomeActivity" />

        <activity android:name=".Activity.StartScreenActivity" />

        <activity android:name=".Activity.CalorieTableActivity" />

        <activity android:name=".Activity.WorkoutActivity" />

        <activity android:name=".Activity.AboutAppActivity" />

        <activity android:name=".Activity.AboutUserActivity" />

        <activity android:name=".Activity.DrawerActivity" />

       <!-- <activity android:name=".SearchableActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>-->

        <!-- Other activities and configuration settings -->
    </application>
</manifest>
